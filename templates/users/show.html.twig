{% extends 'base.html.twig' %}
{% block body %}
    <p>ID: {{ user.id }}</p>
    <p>Name: {{ user.name }}</p>
    <p>Name: {{ user.surname }}</p>
    <p>Nickname: {{ user.nickname }}</p>
{% endblock %}
