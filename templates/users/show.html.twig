{% extends 'base.html.twig' %}
{% block body %}
    <p><a href="{{ path('app_subscribe', {'id': user.id}) }}">Subscribe</a></p>
    <p>ID: {{ user.id }}</p>
    <p>Name: {{ user.name }}</p>
    <p>Name: {{ user.surname }}</p>
    <p>Nickname: {{ user.nickname }}</p>
{% endblock %}
